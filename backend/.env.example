# === App Database (MySQL — stocke config + utilisateurs locaux) ===
APP_DB_HOST=localhost
APP_DB_PORT=3306
APP_DB_NAME=glpi_dashboard_app
APP_DB_USER=dashboard_user
APP_DB_PASSWORD=your_password_here

# === JWT ===
JWT_SECRET=change_this_to_a_long_random_string_in_production
JWT_EXPIRES_IN=8h

# === Serveur ===
PORT=4000
NODE_ENV=development
